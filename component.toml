Application = "GLOBAL.ortelius.saas.ortelius"
Application_Version = "10.0.0"
DeployEnv = "GLOBAL.ortelius.saas.aks-cluster"

Name = "GLOBAL.ortelius.saas.ms-compitem-crud"
Variant = "${BRANCH_NAME}"
Version = "v10.0.0.${BUILDNUM}-g${SHORT_SHA}"

[Export]
     IMAGE_TAG = "${Variant}-${Version}"
     COMPONENT_DOCKERREPO="${DockerRepo}"

[Attributes]
     BuildId = "${BUILD_ID}"
     BuildUrl = "https://console.cloud.google.com/cloud-build/builds/${BUILD_ID}?project=${PROJECT_ID}"
     Chart = "chart/ms-compitem-crud"
     ChartNameSpace = "ortelius"
     CustomAction = "GLOBAL.ArgoHelmAction"
     DockerBuildDate = "${BLDDATE}"
     DockerRepo = "quay.io/ortelius/ms-compitem-crud"
     DockerSha = "${DIGEST}"
     DockerTag = "${IMAGE_TAG}"
     GitBranch = "${BRANCH_NAME}"
     GitCommit = "${SHORT_SHA}"
     GitRepo = "${GIT_REPO}"
     GitTag = "${TAG_NAME}"
     GitUrl = "${GIT_URL}"
     License = "/workspace/LICENSE"
     Readme = "/workspace/README.md"
     ServiceOwner = "GLOBAL.Santa Fe Software.ETaylor"
     ArgoGitRepo = "git@github.com:ortelius/ortelius-ms-compitem-crud.git"
     ArgoGitBranch = "${GitBranch}"
     HelmValues = "chart/ms-compitem-crud/values.yaml"
     DeployAlways = "Y"
